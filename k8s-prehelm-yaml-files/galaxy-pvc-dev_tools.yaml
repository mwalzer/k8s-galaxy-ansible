kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: galaxy-dev-tools
spec:
  storageClassName: manual
  volumeName: k8s-galaxy-dev.tools-volume
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
