kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: galaxy-dev
spec:
  storageClassName: manual
  volumeName: k8s-galaxy-dev-volume
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
